{% extends 'base.j2' %} {% block header %}
<div class="container">
  <header>
    <h1 class="site-title">
      <a href="index.html">{{ sitemeta.title }}</a>
    </h1>
    <ul>
      {% for menu in menus %}
      <li>
        <a href="{{ menu }}.html">{{ menu }}</a>
      </li>
      {% endfor %}
    </ul>
  </header>
</div>
{% endblock header %} {% block content%}
<div class="container">
  {{ body }}

  <ul>
    {% for link, title in current_articles.items() %}
    <li>
      <a href="{{ link }}">{{ title }}</a>
    </li>
    {% endfor %}
  </ul>
</div>
{% endblock content %}
